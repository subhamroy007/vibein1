import { SafeAreaView } from "react-native-safe-area-context";
import { layoutStyle } from "../../../styles";

export default function Discover() {
  return <SafeAreaView style={[layoutStyle.flex_1]}></SafeAreaView>;
}
